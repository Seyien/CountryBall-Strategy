# Media

Repository media that is not a Unity asset. Nothing in this folder is imported
by the engine; it exists only for the root `README.md`.

Expected first file: **`gameplay.gif`** — a short capture of a battle turn,
recorded once the scene wiring lands.

When the file exists, open the root `README.md` and replace the single italic
line at the top with the image line quoted in the HTML comment directly above
it. That is the whole operation; nothing else references this folder.
